{
  "name": "@taskedo/cloud",
  "version": "0.1.0",
  "description": "Cloud integrations (Cloud Tasks, DLQ helpers) for Taskedo",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@taskedo/common": "file:../common"
  }
}
